import PageTitle from '@components/pages/components/PageTitle';
import FormContainer from '../components/FormContainer';

const CloseElectionsForm = () => (
  <FormContainer>
    <PageTitle>Close elections page</PageTitle>
  </FormContainer>
);

export default CloseElectionsForm;
