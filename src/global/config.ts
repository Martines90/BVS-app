/* eslint-disable import/prefer-default-export */
import { DEFAULT_API_PORT, DEFAULT_PORT } from './constants/general';

export const apiBaseUrl = `http://localhost:${DEFAULT_API_PORT}`;

export const baseUrl = `http://localhost:${DEFAULT_PORT}`;
